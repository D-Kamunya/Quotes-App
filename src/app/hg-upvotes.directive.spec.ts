import { HgUpvotesDirective } from './hg-upvotes.directive';

describe('HgUpvotesDirective', () => {
  it('should create an instance', () => {
    const directive = new HgUpvotesDirective();
    expect(directive).toBeTruthy();
  });
});
